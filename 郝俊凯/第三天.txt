    今天任务很简单，就是听别人讲，然后提问题，首先呢先说几个问题，1：用户特征是从1开始，不是从0开始，求总个数为何加1,2：movies_categories_max = genres_num电影类型传过来19个，单注释18个，那个是正确的，3：文本卷积数量，filter_num = 8 ，这是固定的吗,还是随机取。
	movie__nn.py讲解，首先用get_inputs()初始化占位符
	get_movie_id_embed_layer（电影ID全连接）
    	get_movie_categories_embed_layer（）#对电影类型的		多个嵌入向量做加和
	get_movie_cnn_layer（）#电影名卷积
	网络的第一层是词嵌入层，由每一个单词的嵌入向量组成的嵌入矩阵。下一层使用多个不同尺寸（窗口大小）
	的卷积核在嵌入矩阵上做卷积，窗口大小指的是每次卷积覆盖几个单词。这里跟对图像做卷积不太一样，
	图像的卷积通常用2x2、3x3、5x5之类的尺寸，而文本卷积要覆盖整个单词的嵌入向量，所以尺寸是
	（单词数，向量维度），比如每次滑动3个，4个或者5个单词。
	第三层网络是max pooling得到一个长向量，最后使用dropout做正则化，最终得到了电影Title的特征。
	get_movie_feature_layer（）电影特征全连接
