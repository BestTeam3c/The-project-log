项目日志：    今天主要看了数据预处理过程，在数据预处理中，          首先是处理用户数据，用 pd.read_table（）读取
	  然后是将没有用的字段Zip-code过滤掉，保存一个源数           据，然后就是对性别一栏操作，将性别{'F':0,'M':1}            替换成数值类型0，1，将年龄转为字典，分7个段，
	      对电影字段操作，将年份过滤掉，用的是正则，	          re.compile加match加group：#re.match函数尝试从字	          符串的开头开始匹配一个模式，如果匹配成功，返回一           个匹配成功的对象#正则表达式中，group（）用来提出           分组截获的字符串 ，（）用来分组，group(1)是要第          一个部分内容，movies['Title'].str.split()是转为            列表，用一个集合.update()分割为单词放在集合中，            无重复，首先创建文本到数字的字典，然后将Title中          的描述转成数字的列表，填充单词
	       rating数据处理，只需要将timestamps舍去，保            留其他属性即可
	       影类型转为数字字典，和电影字段处理相同，然          后将处理的字段存成.p文件，方便其他.py文件调取

	pickle.dump(obj, file, [,protocol])
	注释：序列化对象，将对象obj保存到文件file中去。参数protocol是序列化模式，默认是0
	file表示保存到的类文件对象，file必须有write()接口，file可以是一个以'w'打开的文件
	或者是一个StringIO对象，也可以是任何可以实现write()接口的对象。